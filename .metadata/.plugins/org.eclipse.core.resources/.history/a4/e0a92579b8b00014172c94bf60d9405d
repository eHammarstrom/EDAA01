package mountain;

import fractal.*;

public class Side {
	private Point a;
	private Point b;

	public Side(Point a, Point b) {
		this.a = a;
		this.b = b;
	}
	
	public Point getMid() {
		return new Point((a.getX() + b.getX()) / 2, (a.getY() + b.getY()) / 2);
	}
}