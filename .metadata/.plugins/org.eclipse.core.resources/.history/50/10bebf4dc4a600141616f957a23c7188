package set;

import java.util.Iterator;

public class UniqueClass {
	
	public static void main(String[] args) {
		int[] test = { 0, 3, 2, 4, 7, 5 };
		
		int[] asd = uniqueElements(test);
		
		for (int i : asd) {
			System.out.print(asd[i]);
		}
	}
	
	public static int[] uniqueElements(int[] ints) {
		MaxSet<Integer> ms = new MaxSet<Integer>();
		int[] ret = new int[ints.length];
		
		for (int i : ints) {
			ms.add(i);
		}
		
		System.out.print(ms.getMax());
		
		Iterator<Integer> iter = ms.iterator();
		
		int q = 0;
		while (iter.hasNext()) {
			int temp = iter.next();
			
			ret[q] = temp;
			
			ms.remove(temp);
					
			q++;
		}
		
		for (int i : ms) {
			ret[i] = ms.getMax();
			//ms.remove(ms.getMax());
			iter.remove();
		}
		
		return ret;
	}
}
